kind = "web"
previewPath = "/"
title = "VPN Control Panel"
version = "1.0.0"
id = "artifacts/vpn-panel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19446

[services.development]
run = "pnpm --filter @workspace/vpn-panel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vpn-panel", "run", "build" ]
publicDir = "artifacts/vpn-panel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19446"
BASE_PATH = "/"
